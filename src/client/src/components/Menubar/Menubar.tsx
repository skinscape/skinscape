import React from "react";

type MenubarProps = {
    children: Array<React.ReactElement>,
};

export const Menubar: React.FC<MenubarProps> = ({
    children,
}) => {


    return (
        <></>
    );
};